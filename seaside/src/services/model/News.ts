export type News = {
    id: string;
    title: string;
    description: string;
    publication: string;
    image: string;
    link: string;
    feeds: string[];
    read: boolean;
    shared: boolean;
}
