type AtomFeed {
    id: ID,
    title: String,
    description: String,
    author: String,
    link: URI!
}

type AtomEntry {
    id: ID,
    title: String,
    description: String,
    author: String,
    link: URI!
    feeds: [String]
}

type ScrapingError {
    since: String,
    lastTime: String,
    status: Int,
    label: String,
}

extend type Query {
    scrapFeedHeader(link: URI): AtomFeed
}

extend type Mutation {
    scrapSimpleNews(uri: URI): Void
}