extend type Mutation {
    login(username: String!, password: String!, remember: Boolean): User
    logout: Void
    refreshSession: Session
}

extend type Query {
    askForPasswordReset(identifier: String): Void
    currentUser: User
}

